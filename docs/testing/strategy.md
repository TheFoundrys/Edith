# Testing

- Unit: `tests/unit` (shared roles/utils)
- Integration: `tests/integration/api` (supertest against Express app)
- E2E: placeholder folders under `tests/e2e` for Playwright/Cypress later

Run: `npm test`
